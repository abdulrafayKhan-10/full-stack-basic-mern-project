{
  "name": "full-stack-basic-mern-project",
  "version": "1.0.0",
  "description": "This is the basic Full Stack Mern Project",
  "main": "index.js",
  "scripts": {
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "dev": "SET NODE_ENV=development && nodemon backend/server.js",
    "start": "SET NODE_ENV=production && node backend/server.js"
  },
  "type": "module",
  "keywords": [],
  "author": "", 
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.6.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
